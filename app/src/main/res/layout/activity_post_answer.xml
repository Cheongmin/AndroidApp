<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite"
    tools:context=".PostAnswerActivity">

    <include
        layout="@layout/include_toolbar" />

    <ScrollView
        android:id="@+id/sv_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/tv_edit_answer"
        app:layout_constraintTop_toBottomOf="@+id/toolbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="36dp"
            android:orientation="vertical">

            <include
                android:id="@+id/question_view"
                layout="@layout/layout_question" />

            <android.support.v7.widget.RecyclerView
                android:id="@+id/rv_answers"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layoutManager="android.support.v7.widget.LinearLayoutManager"/>

        </LinearLayout>

    </ScrollView>

    <TextView
        android:id="@+id/tv_edit_answer"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:paddingLeft="16dp"
        android:paddingRight="0dp"
        android:background="@color/colorGray1"
        android:gravity="center"
        android:text="@string/edit_answer_placeholder"
        android:textColor="@color/colorGray6"
        android:textSize="14sp"
        android:textAlignment="textStart"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btn_post_answer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintWidth_percent="0.7"
        tools:ignore="RtlCompat" />

    <TextView
        android:id="@+id/btn_post_answer"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:gravity="center"
        android:background="@color/colorPrimary"
        android:text="작성하기"
        android:textSize="14dp"
        android:textColor="@color/colorWhite"
        android:textAlignment="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tv_edit_answer"
        app:layout_constraintWidth_percent="0.3" />

</android.support.constraint.ConstraintLayout>